<Window x:Class="Voicy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:Voicy.Helpers"
        Title="VOICY" Height="320" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Icon="Resources/voicy.ico"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:BoolToStatusColorConverter x:Key="BoolToColor"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="VOICY" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Ellipse Width="12" Height="12" Margin="12,0,0,0" VerticalAlignment="Center"
                     x:Name="StatusIndicator">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#9E9E9E"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsListening}" Value="True">
                                <Setter Property="Fill" Value="#4CAF50"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                <Setter Property="Fill" Value="#FFC107"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="{Binding StatusText}" Margin="8,0,0,0" VerticalAlignment="Center"
                       FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <!-- Mode Selection -->
        <GroupBox Grid.Row="1" Header="Mode" Margin="0,0,0,8">
            <StackPanel Margin="4">
                <RadioButton Content="Toggle (press hotkey to start/stop)"
                             IsChecked="{Binding IsToggleMode}" Margin="0,2"
                             Checked="Mode_Changed" Tag="Toggle"/>
                <RadioButton Content="Push-to-Talk (hold hotkey)"
                             IsChecked="{Binding IsPushToTalkMode}" Margin="0,2"
                             Checked="Mode_Changed" Tag="PushToTalk"/>
                <RadioButton Content="Continuous (always listening)"
                             IsChecked="{Binding IsContinuousMode}" Margin="0,2"
                             Checked="Mode_Changed" Tag="Continuous"/>
            </StackPanel>
        </GroupBox>

        <!-- Hotkey Display -->
        <TextBlock Grid.Row="2" Margin="0,0,0,8" FontSize="12" Foreground="Gray">
            <Run Text="Hotkey: "/>
            <Run Text="{Binding HotkeyDisplay, Mode=OneWay}" FontWeight="SemiBold"/>
        </TextBlock>

        <!-- Last Transcription -->
        <GroupBox Grid.Row="3" Header="Last Transcription" Margin="0,0,0,8">
            <TextBox Text="{Binding LastTranscription, Mode=OneWay}"
                     IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" BorderThickness="0"
                     Margin="4"/>
        </GroupBox>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Settings" Command="{Binding OpenSettingsCommand}"
                    Width="80" Margin="0,0,8,0"/>
            <Button Content="Minimize to Tray" Click="MinimizeToTray_Click" Width="110"/>
        </StackPanel>
    </Grid>
</Window>
